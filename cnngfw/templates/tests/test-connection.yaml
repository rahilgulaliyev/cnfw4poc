apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cnngfw.fullname" . }}-test-connection"
  labels:
    {{- include "cnngfw.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cnngfw.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
