apiVersion: v1
kind: Service
metadata:
  name: {{ include "cnngfw.fullname" . }}
  labels:
    {{- include "cnngfw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cnngfw.selectorLabels" . | nindent 4 }}
